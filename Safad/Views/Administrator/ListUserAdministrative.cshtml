@{
    Layout = "~/Views/Shared/_LayoutAdministrative.cshtml";
}

@model UserAdministrative // Tu modelo que contiene los datos del profesional

<div class="container">
    <h2 class="text-center">Mis Datos</h2> <!-- Centra el encabezado -->
    <div class="row justify-content-center">
        <!-- Centra la fila -->
        <div class="col-md-6">
            <!-- Ajusta el tamaño de la columna -->
            <div class="card">
                <div class="card-body">
                    <div>
                        <label>Nombre Completo: </label>
                        <span>@Model.NameAdministrative</span>
                    </div>
                    <div>
                        <label>DNI: </label>
                        <span>@Model.DniAdministrative</span>
                    </div>
                    <div>
                        <label>Teléfono: </label>
                        <span>@Model.Cellphone</span>
                    </div>
                    <div>
                        <label>Dirección: </label>
                        <span>@Model.Address</span>
                    </div>
                    <!-- Agrega más campos si es necesario -->
                </div>
                <div class="card-footer text-center">
                    <!-- Botón de Actualizar -->
                    <a asp-controller="Administrator" asp-action="EditUserAdministrative" asp-route-id="@Model.UserAdministrativeId" class="btn btn-primary">
                        Actualizar
                    </a>
                    <!-- Botón de Eliminar -->
                    <form asp-controller="Administrator" asp-action="Delete" asp-route-id="@Model.UserAdministrativeId" method="post" style="display:inline;">
                        <button type="submit" class="btn btn-danger" onclick="return confirm('¿Estás seguro de que deseas eliminar este profesional?');">
                            Eliminar
                        </button>
                    </form>
                </div>

            </div>
        </div>
    </div>
</div>

